import MailManagerClient from './MailManagerClient'

export const metadata = {
    title: 'TEKPIK — Mail Manager',
    description: 'Send broadcasts and manage email templates',
}

export default function MailManagerPage() {
    return <MailManagerClient />
}
